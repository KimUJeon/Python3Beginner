# 모듈을 사용하는 가장 큰 이유는 '효율성'이다.
# 두번째 이유로는 타인이 만들어둔 코드를 자신의 코드에 활용할 수 있다는 점
* 모듈은 프로그램의 꾸러미

# import 사용 방법은 <import 모듈 이름> 이다. 포함하려는 파일이 현재 파일과 같은 디렉터리에 있어야 한다.

--------------------------------------------------------------------------------------------------
(예시)

import random

a = random.random()
b = random.randrange(1, 10)
c = ['a', 'b', 'c', 'd']
d = random.choice(c)

print(a, b, c, d)
